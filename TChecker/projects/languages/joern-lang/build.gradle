apply plugin: 'java'
repositories { mavenCentral() }

task copyLang(type:Copy) {
  from "${project.projectDir}/src/main/groovy/"
  into "${project.projectDir}/../../octopus/languages/joern/"
}

build.dependsOn copyLang
